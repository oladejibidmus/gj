
  # Build React Application

  This is a code bundle for Build React Application. The original project is available at https://www.figma.com/design/22jQCVZjB5kDvuRyid80d3/Build-React-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  